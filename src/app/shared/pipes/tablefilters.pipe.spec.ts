import { TablefiltersPipe } from './tablefilters.pipe';

describe('TablefiltersPipe', () => {
  it('create an instance', () => {
    const pipe = new TablefiltersPipe();
    expect(pipe).toBeTruthy();
  });
});
